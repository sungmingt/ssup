spring:
  config:
    activate:
      on-profile: local
    import:
      - ssup-security/application.yml
      - ssup-security/application-datasource.yml.yml.yml.yml
      - ssup-security/application-cloud.yml

---

spring:
  config:
    activate:
      on-profile: prod
    import:
      - ssup-security/application.yml
      - ssup-security/application-datasource.yml.yml.yml.yml
      - ssup-security/application-cloud.yml